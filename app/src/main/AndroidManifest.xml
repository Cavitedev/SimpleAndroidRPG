<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simplerpg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">
        <activity android:name=".application.partyCreation.PartyCreationActivity"></activity>
        <activity android:name=".application.partyCreation.HeroImageSelectionActivity" />
        <activity android:name=".application.partyGrid.PartyGridFormationSelectionActivity" />
        <activity android:name=".application.battle.BattleActivity" />
        <activity android:name=".application.SkillsTreesActivity" />
        <activity android:name=".application.heroProfile.HeroProfileActivity" />
        <activity
            android:name=".application.MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>